name: Build and Push Docker Image

on:
  push:
    branches: [ main ]
    tags: [ 'v*' ]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read
  packages: write
  id-token: write

concurrency:
  group: build-and-push-${{ github.ref }}
  cancel-in-progress: true

env:
  REGISTRY_HOST: registry.agentsmp.com
  REGISTRY_NAMESPACE: agentsmp
  BUILD_CONTEXT: .
  DOCKERFILE_PATH: ./Dockerfile

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    env:
      NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
      NEXT_PUBLIC_SUPABASE_ANON_KEY: ${{ secrets.NEXT_PUBLIC_SUPABASE_ANON_KEY }}
      NEXT_PUBLIC_BASE_API_URL: ${{ secrets.NEXT_PUBLIC_BASE_API_URL }}
      LANGSMITH_API_KEY: ${{ secrets.LANGSMITH_API_KEY }}
      NEXT_PUBLIC_USE_LANGSMITH_AUTH: ${{ vars.NEXT_PUBLIC_USE_LANGSMITH_AUTH }}
      NEXT_PUBLIC_DEPLOYMENTS: ${{ secrets.NEXT_PUBLIC_DEPLOYMENTS }}
      NEXT_PUBLIC_RAG_API_URL: ${{ vars.NEXT_PUBLIC_RAG_API_URL }}
      INTERNAL_RAG_URL: ${{ vars.INTERNAL_RAG_URL }}
      NEXT_PUBLIC_MCP_SERVER_URL: ${{ vars.NEXT_PUBLIC_MCP_SERVER_URL }}
      NEXT_PUBLIC_MCP_SERVER_INTERNAL_URL: ${{ vars.NEXT_PUBLIC_MCP_SERVER_INTERNAL_URL }}
      NEXT_PUBLIC_MCP_AUTH_REQUIRED: ${{ vars.NEXT_PUBLIC_MCP_AUTH_REQUIRED }}
      NEXT_PUBLIC_GOOGLE_AUTH_DISABLED: ${{ vars.NEXT_PUBLIC_GOOGLE_AUTH_DISABLED }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Compute image name
        id: imagename
        shell: bash
        run: |
          REPO="${{ github.event.repository.name }}"
          NS="${{ env.REGISTRY_NAMESPACE }}"
          REPO_LC="${REPO,,}"
          IMAGE_NAME="${NS}/${REPO_LC}"
          IMAGE_REF="${{ env.REGISTRY_HOST }}/${IMAGE_NAME}"
          echo "IMAGE_NAME=${IMAGE_NAME}" >> "$GITHUB_ENV"
          echo "IMAGE_REF=${IMAGE_REF}"   >> "$GITHUB_ENV"
          echo "DOCKER_CONTEXT=default"   >> "$GITHUB_ENV"

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY_HOST }}
          username: ${{ secrets.REGISTRY_USERNAME }}
          password: ${{ secrets.REGISTRY_PASSWORD }}

      - name: Extract Docker metadata (tags, labels)
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.IMAGE_REF }}
          tags: |
            # release tags like v1.2.3 -> 1.2.3, 1.2, 1, latest
            type=ref,event=tag
            type=semver,pattern={{version}},prefix=,enable=${{ startsWith(github.ref, 'refs/tags/v') }}
            type=semver,pattern={{major}}.{{minor}},prefix=,enable=${{ startsWith(github.ref, 'refs/tags/v') }}
            type=semver,pattern={{major}},prefix=,enable=${{ startsWith(github.ref, 'refs/tags/v') }}
            type=raw,value=latest,enable=${{ startsWith(github.ref, 'refs/tags/v') }}
            # main branch -> main and main-<shortSHA>
            type=raw,value=main,enable=${{ github.ref == 'refs/heads/main' }}
            type=sha,format=short,prefix=main-,enable=${{ github.ref == 'refs/heads/main' }}

      # Build only on PRs (no push)
      - name: Build image (PR only)
        if: github.event_name == 'pull_request'
        uses: docker/build-push-action@v6
        with:
          context: ${{ env.BUILD_CONTEXT }}
          file: ${{ env.DOCKERFILE_PATH }}
          push: false
          platforms: linux/amd64
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          build-args: |
            NODE_ENV=production
            NEXT_PUBLIC_BASE_API_URL=${{ env.NEXT_PUBLIC_BASE_API_URL }}
            LANGSMITH_API_KEY=${{ env.LANGSMITH_API_KEY }}
            NEXT_PUBLIC_USE_LANGSMITH_AUTH=${{ env.NEXT_PUBLIC_USE_LANGSMITH_AUTH }}
            NEXT_PUBLIC_DEPLOYMENTS=${{ env.NEXT_PUBLIC_DEPLOYMENTS }}
            NEXT_PUBLIC_RAG_API_URL=${{ env.NEXT_PUBLIC_RAG_API_URL }}
            INTERNAL_RAG_URL=${{ env.INTERNAL_RAG_URL }}
            NEXT_PUBLIC_MCP_SERVER_URL=${{ env.NEXT_PUBLIC_MCP_SERVER_URL }}
            NEXT_PUBLIC_MCP_SERVER_INTERNAL_URL=${{ env.NEXT_PUBLIC_MCP_SERVER_INTERNAL_URL }}
            NEXT_PUBLIC_MCP_AUTH_REQUIRED=${{ env.NEXT_PUBLIC_MCP_AUTH_REQUIRED }}
            NEXT_PUBLIC_GOOGLE_AUTH_DISABLED=${{ env.NEXT_PUBLIC_GOOGLE_AUTH_DISABLED }}
            NEXT_PUBLIC_SUPABASE_URL=${{ env.NEXT_PUBLIC_SUPABASE_URL }}
            NEXT_PUBLIC_SUPABASE_ANON_KEY=${{ env.NEXT_PUBLIC_SUPABASE_ANON_KEY }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      # Build & push on main branch, tags, or manual dispatch
      - name: Build and push image
        if: github.event_name == 'workflow_dispatch' || github.ref == 'refs/heads/main' || startsWith(github.ref, 'refs/tags/v')
        uses: docker/build-push-action@v6
        with:
          context: ${{ env.BUILD_CONTEXT }}
          file: ${{ env.DOCKERFILE_PATH }}
          push: true
          platforms: linux/amd64
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          build-args: |
            NODE_ENV=production
            NEXT_PUBLIC_BASE_API_URL=${{ env.NEXT_PUBLIC_BASE_API_URL }}
            LANGSMITH_API_KEY=${{ env.LANGSMITH_API_KEY }}
            NEXT_PUBLIC_USE_LANGSMITH_AUTH=${{ env.NEXT_PUBLIC_USE_LANGSMITH_AUTH }}
            NEXT_PUBLIC_DEPLOYMENTS=${{ env.NEXT_PUBLIC_DEPLOYMENTS }}
            NEXT_PUBLIC_RAG_API_URL=${{ env.NEXT_PUBLIC_RAG_API_URL }}
            INTERNAL_RAG_URL=${{ env.INTERNAL_RAG_URL }}
            NEXT_PUBLIC_MCP_SERVER_URL=${{ env.NEXT_PUBLIC_MCP_SERVER_URL }}
            NEXT_PUBLIC_MCP_SERVER_INTERNAL_URL=${{ env.NEXT_PUBLIC_MCP_SERVER_INTERNAL_URL }}
            NEXT_PUBLIC_MCP_AUTH_REQUIRED=${{ env.NEXT_PUBLIC_MCP_AUTH_REQUIRED }}
            NEXT_PUBLIC_GOOGLE_AUTH_DISABLED=${{ env.NEXT_PUBLIC_GOOGLE_AUTH_DISABLED }}
            NEXT_PUBLIC_SUPABASE_URL=${{ env.NEXT_PUBLIC_SUPABASE_URL }}
            NEXT_PUBLIC_SUPABASE_ANON_KEY=${{ env.NEXT_PUBLIC_SUPABASE_ANON_KEY }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Show image references
        if: success() && (github.event_name == 'workflow_dispatch' || github.ref == 'refs/heads/main' || startsWith(github.ref, 'refs/tags/v'))
        run: |
          echo "Published tags:"
          echo "${{ steps.meta.outputs.tags }}"
